version: 2

sources:
  - name: raw
    description: "Tables produced by the Python pipeline in the DuckDB database."
    schema: main
    tables:
      - name: transactions
        description: "Cleaned transaction lines (returns flagged)."
      - name: customer_clusters
        description: "Per-customer KMeans segment assignments."
